// namespace AvaTerminal3.Helpers;

// public static class ApiClient
// {
//     // will be set exactly once in Program.cs
//     internal static IHttpClientFactory HttpClientFactory { get; private set; } = null!;

//     public static void Initialize(IHttpClientFactory factory)
//         => HttpClientFactory = factory;

//     public static HttpClient CreateClient(string name)
//         => HttpClientFactory.CreateClient(name);
// }
